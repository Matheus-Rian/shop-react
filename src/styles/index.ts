import { createStitches } from "@stitches/react";

export const {
  config,
  createTheme,
  styled,
  getCssText,
  globalCss,
  keyframes,
  theme,
  css,
} = createStitches({
  theme: {
    colors: {
      gray: '#222'
    }
  }
})